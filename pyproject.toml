[project]
name = "sailing-track-visualizer"
version = "0.1.0"
description = "A Flask app to visualize sailing GPS tracks from JSON and GPX files"
requires-python = ">=3.9"
dependencies = [
    "flask>=3.0.0",
    "gpxpy>=1.6.0",
]
